using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Windows.Controls;

namespace MPLite
{
    /*public static class LV_Playlist_Extensions
    {
        // NOTICE: this ListView extension is for ones comes from "System.Windows.Controls", 
        // not "System.Windows.Forms".
        public static void AddTracks(this ListView lv, string[] filePaths)
        {
            foreach (string filePath in filePaths)
            {
                string trackName = System.IO.Path.GetFileNameWithoutExtension(filePath);
                TrackInfo trackInfo = new TrackInfo { TrackName = trackName, TrackPath = filePath };
                lv.Items.Add(trackInfo);
            }
        }
    }*/
}
